{
  "name": "mobius-brain",
  "version": "0.1.0",
  "description": "System-wide cognition and orchestration layer for Mobius.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/**/*.test.js",
    "validate": "npm run build && npm run test"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
